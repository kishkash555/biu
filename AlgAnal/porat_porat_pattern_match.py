class process():
    def __init__(self, signatures):
        self.text_signature = []
        self.signatures = signatures
        self.next_power_of_2=1
    def update(char):
        self.text_signature.append(char)
        if len(self.text_signature) ==  self.next_power_of_2:
            if 